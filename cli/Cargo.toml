[package]
name = "ripple-cli"
description = "Ripple CLI daemon and tooling"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "ripple"
path = "src/main.rs"

[dependencies]
ripple-core = { path = "../core" }
